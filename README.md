# Tintas reais para Minetest

## Configurações
* `tintas_reais:crafting = false` altera as receitas das tintas (padrão é `false`)
* `tintas_reais:textures = true` altera as texturas das tintas (padrão é `true`)
